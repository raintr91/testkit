id: TC-EXAMPLE-VALID
title: Example flow with valid input should succeed
summary: Ví dụ — luồng hợp lệ đi đến trạng thái thành công.
story: |
  Given điều kiện đầu vào hợp lệ
  When thực hiện thao tác chính
  Then thấy trạng thái thành công
refs:
  module: CMP-01-auth
  surface: admin
  scenario: SC-EXAMPLE
  example: EX-EXAMPLE-01
  screen: W-AD-AUTH-001
coverage: [happy]
priority: high
automation: automated
tags: [smoke, e2e, ui]
dimensions:
  business: [workflow]
  technical: [ui]
  quality: []
preconditions: []
testData: {}
steps: []
expected: Success state is visible
postcondition: []
feature: example-slug
genType: e2e
route:
  path: /example
  auth: guest
testIds:
  required: []
